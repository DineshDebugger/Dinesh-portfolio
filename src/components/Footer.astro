---
import type { SiteConfig } from "@types";

type Props = Pick<SiteConfig, "author" | "socialLinks">;

const { author, socialLinks } = Astro.props;
const currentYear = new Date().getFullYear();
---

<style>
  @keyframes waveShimmer {
    0% {
      background-position: -200% center;
    }
    50% {
      background-position: 200% center;
    }
    100% {
      background-position: -200% center;
    }
  }
</style>

<footer class="w-full bg-black px-6 pt-14 pb-10 text-white">
  <!-- Top Section -->
  <div class="mb-10 text-center">
    <a
      href="mailto:dineshrvd1403@gmail.com?subject=Let's%20Work%20Together&body=Hi%20Dinesh,"
      class="mt-3 mb-4 inline-block bg-gradient-to-r from-primary via-pink-500 to-purple-500 bg-clip-text text-5xl font-extrabold text-transparent transition duration-500 hover:scale-105 hover:animate-[waveShimmer_3s_ease_infinite] hover:bg-[length:300%_100%]"
    >
      Need a developer?
    </a>
    <p class="text-lg text-neutral-400">
      Let’s work together to build something amazing
    </p>
  </div>

  <!-- Contact Info -->
  <div
    class="mx-auto mb-10 grid max-w-5xl grid-cols-1 gap-12 text-center md:grid-cols-3 md:text-left"
  >
    <div>
      <h3 class="mb-3 text-lg font-semibold">📞 Call Me</h3>
      <p class="text-base text-neutral-300 transition hover:text-white">
        +91 86675 28187
      </p>
    </div>

    <div>
      <h3 class="mb-3 text-lg font-semibold">🌐 Social</h3>
      <ul class="flex justify-center gap-6 text-base md:justify-start">
        {
          socialLinks.map(({ text, href }) => (
            <li>
              <a
                href={href}
                target="_blank"
                class="transition duration-300 hover:text-primary"
              >
                {text}
              </a>
            </li>
          ))
        }
      </ul>
    </div>

    <div>
      <h3 class="mb-3 text-lg font-semibold">☕ Let’s grab a coffee</h3>
      <p class="text-neutral-300 transition hover:text-white">
        dineshrvd1403@gmail.com
      </p>
    </div>
  </div>

  <!-- Copyright -->
  <div
    class="mt-8 border-t border-neutral-800 pt-6 text-center text-sm text-neutral-500"
  >
    {author} © {currentYear}. <span class="text-neutral-400"
      >Designed & Developed by</span
    >
    <span class="font-semibold text-white">Dinesh Babu</span>
  </div>
</footer>

<button
  id="backToTop"
  class="fixed right-8 bottom-8 z-50 flex h-12 w-12 translate-y-4 items-center justify-center rounded-full bg-gradient-to-br from-blue-500 to-purple-600 text-white opacity-0 shadow-lg transition-all duration-300 ease-in-out hover:scale-110 hover:shadow-xl"
  aria-label="Back to top"
>
  <svg
    class="h-6 w-6"
    fill="none"
    stroke="currentColor"
    viewBox="0 0 24 24"
    xmlns="http://www.w3.org/2000/svg"
  >
    <path
      stroke-linecap="round"
      stroke-linejoin="round"
      stroke-width="2"
      d="M5 10l7-7m0 0l7 7m-7-7v18"></path>
  </svg>
</button>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const backToTopBtn: any = document.getElementById("backToTop");

    window.addEventListener("scroll", () => {
      if (window.scrollY > 300) {
        backToTopBtn.classList.remove("opacity-0", "translate-y-4");
        backToTopBtn.classList.add("opacity-75", "translate-y-0");
      } else {
        backToTopBtn.classList.remove("opacity-75", "translate-y-0");
        backToTopBtn.classList.add("opacity-0", "translate-y-4");
      }
    });

    backToTopBtn.addEventListener("click", () => {
      window.scrollTo({ top: 0, behavior: "smooth" });
    });
  });
</script>
